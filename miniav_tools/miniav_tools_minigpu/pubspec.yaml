name: miniav_tools_minigpu
description: GPU compute codecs (WGSL via minigpu) for miniav_tools.
version: 0.5.0
environment:
  sdk: ^3.11.4

dependencies:
  miniav_tools_platform_interface: 0.5.0
  miniav_platform_interface: ^0.5.10
  minigpu_platform_interface: ^1.5.5
  minigpu: ^1.5.5
  gpu_tensor: ^1.5.5
  gpu_pipeline: ^1.5.5
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
  image: ^4.5.0
  benchmark_harness: ^2.3.1
