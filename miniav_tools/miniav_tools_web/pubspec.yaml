name: miniav_tools_web
description: WebCodecs/MediaStreamTrack-backed codecs for miniav_tools (browser).
version: 0.2.15
environment:
  sdk: ^3.11.4

dependencies:
  miniav_tools_platform_interface: 0.2.15
  miniav_platform_interface: ^0.5.6
  minigpu_platform_interface: ^1.4.7
  web: ^1.1.0
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
