# Changelog

## 0.1.0

- Initial release scaffold for the WebCodecs-backed implementation of
  `miniav_tools_platform_interface`. Backend registration plumbing in place;
  encoder/decoder/muxer implementations are tracked separately and will land
  in subsequent versions.
